import React from 'react';
import './ButtonPanel.css';

/**
 * A container with the three buttons for in-app navigation: rerandomize, options, home.
 */
class ButtonPanel extends React.Component {

   render() {
      return (
         <div className='overalldiv'></div>
      );
   }
}

export default ButtonPanel;